# ButterLog service

This is a rust web backend that is suitable to be deployed in cloud run.
It uses postgresql for persistence.

For now it allows users to authenticate via discord and prints 'Hello $discord_handle`.